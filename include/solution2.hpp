#pragma once
#include <iostream>

/**
 * @brief Function to count how many vowels have a string
 * @param toCount is the target string
 * @return the number of wovels spaces
 */
int vowels(std::string toCount);
